Challenge 2

1. Crear un componente FirstApp
2. Devuelve un titulo en la etiqueta h1 y un texto "10" en un etiqueta span
3. Importar e implementar FirstApp

//Componente FirstApp.jsx
import React from "react";

const FirstApp = () => {
  return (
    <div>
      <h1>¡Hola, soy FirstApp!</h1>
      <span>10</span>
    </div>
  );
};

export default FirstApp;


//componente main.jsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import FirstApp from './FirstApp'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <FirstApp value={0}/>  
  </React.StrictMode>
) 

